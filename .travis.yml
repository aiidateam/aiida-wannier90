language: python
python: 2.7
cache: pip
sudo: true
services:
  - postgresql
addons:
  postgresql: "9.5"
  apt: # copied from wannier90's travis.yml
    packages:
      - gfortran
      - libblas-dev
      - liblapack-dev
install:
  - pip install -U 'pip<19'
  - pip install git+https://github.com/aiidateam/aiida_core.git@v1.0.0a4#egg=aiida_core
  - pip install git+https://github.com/greschd/aiida_pytest@support_alpha_4#egg=aiida_pytest
  - pip install .[test,dev,docs]
env:
  - TEST_TYPE="pre-commit"
  - TEST_TYPE="tests"
script:
  - ./.travis-data/test_script.sh
